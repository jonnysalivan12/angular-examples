{
  "$schema": "http://json-schema.org/schema",
  "$id": "AddAsyncStateActionSchematic",
  "title": "Add a new async action into selected state",
  "type": "object",
  "description": "Initialise a new async state action",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of new async action",
      "x-prompt": "Action name:",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path at which to create the component file, relative to the current workspace. Default is a folder with the same name as the component in the project root.",
      "$default": {
        "$source": "workingDirectory"
      },
      "visible": false
    },
    "requestWithPayload": {
      "type": "boolean",
      "description": "Is there a payload for the [request] action?",
      "x-prompt": "Is there a payload for the [request] action?",
      "default": false,
      "$default": {
        "$source": "argv",
        "index": 1
      }
    },
    "successWithPayload": {
      "type": "boolean",
      "description": "Is there a payload for the [success] action?",
      "x-prompt": "Is there a payload for the [success] action?",
      "default": false,
      "$default": {
        "$source": "argv",
        "index": 2
      }
    },
    "failureWithPayload": {
      "type": "boolean",
      "description": "Is there a payload for the [failure] action?",
      "x-prompt": "Is there a payload for the [failure] action?",
      "default": false,
      "$default": {
        "$source": "argv",
        "index": 3
      }
    }
  }
}
